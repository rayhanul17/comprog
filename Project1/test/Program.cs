//Codeforces
int t = int.Parse(Console.ReadLine());
while (t-- > 0)
{
    int n = int.Parse(Console.ReadLine());
    for (int i = 0; i < n; i++)
        Console.Write(n + " ");
    Console.WriteLine();
}